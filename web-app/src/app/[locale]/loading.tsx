export default function Loading() {
    // Return null to avoid an intrusive loading screen during route transitions.
    // The initial splash screen is handled by the SplashScreen component in layout.tsx
    return null;
}